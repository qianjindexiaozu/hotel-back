<mapper namespace="com.example.UserMapper">

    <resultMap id="UserMap" type="User">
        <id property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="idNumber" column="id_number"/>
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="regTime" column="reg_time"/>
        <result property="userPic" column="user_pic"/>
    </resultMap>

    <select id="selectAllUsers" resultMap="UserMap">
        SELECT user_id, name, gender, id_number, phone, password, role, reg_time, user_pic
        FROM users
    </select>

    <select id="selectUserById" parameterType="Integer" resultMap="UserMap">
        SELECT user_id, name, gender, id_number, phone, password, role, reg_time, user_pic
        FROM users WHERE user_id = #{userId}
    </select>

    <insert id="insertUser" parameterType="User">
        INSERT INTO users (name, gender, id_number, phone, password, role, reg_time, user_pic)
        VALUES (#{name}, #{gender}, #{idNumber}, #{phone}, #{password}, #{role}, #{regTime}, #{userPic})
    </insert>

    <update id="updateUser" parameterType="User">
        UPDATE users
        SET name = #{name}, gender = #{gender}, id_number = #{idNumber}, phone = #{phone},
        password = #{password}, role = #{role}, reg_time = #{regTime}, user_pic = #{userPic}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser" parameterType="Integer">
        DELETE FROM users WHERE user_id = #{userId}
    </delete>

</mapper>
